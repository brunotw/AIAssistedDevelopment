# Copilot Repository Instructions

## Skill Routing Rule

When editing any HTML file, if the task adds or modifies a `<button>` element, the assistant must apply the `button-standardizer` skill from `.skills/button-standardizer/SKILL.md`.

Do not generate final button HTML until the skill rules are followed.